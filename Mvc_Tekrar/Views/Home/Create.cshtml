@model Mvc_Tekrar.Models.Blog
@{ 
    Layout = "_Layout";
}
<html xmlns="http://www.w3.org/1999/xhtml">
<head>
    <link href="~/bootstrap/css/bootstrap.css" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <title></title>

    <style>
        .buttonCategories {
            border: hidden;
            margin-bottom: 10px;
            padding-right: 1rem;
            padding-left: 1rem;
            display: inline-block;
            font-size: 1rem;
            font-weight: 600;
            color: rgb(236, 106, 42);
        }

        footer {
            bottom: 0px;
            left: 0px;
            width: 100%;
            height: 150px;
            padding: 15px;
            background-color: #000;
        }
    </style>
</head>
<body>
    <main class="my-3">

        <div class="container">

            <header>
                <div class="jumbotron jumbotron-fluid">
                    <div class="container" style="text-align:center;">
                        <div class="col-12 mx-auto inner">
                            <h1 data-aos="zoom-out-up" data-aos-delay="400" class="title effect-static-text aos-init aos-animate">Add Blog</h1>
                        </div>
                    </div>
                </div>
            </header>
            <form asp-action="Create" method="post">
                <div class="form-group">

                    <label>Blog Title</label>
                    <input type="text" asp-for="BlogTitle" value="@Model.BlogTitle" class="form-control" />
                    <span asp-validation-for="BlogTitle"></span>

                </div>
                <div class="form-group">
                    <label>Category</label>
                    <select asp-for="CategoryId" class="form-control">
                        <option value="" disabled selected> Kategori Seçiniz </option>
                        @foreach (var data in ViewBag.Categories)
                        {
                            <option value="@data.id">@data.CategoryName</option>
                        }

                    </select>
                    <span asp-validation-for="CategoryId"></span>

                </div>
                <button type="submit" class="btn btn-success">Submit</button>
            </form>


        </div>

    </main>
</body>
</html>